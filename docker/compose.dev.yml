services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: kaskita
      POSTGRES_PASSWORD: kaskita
      POSTGRES_DB: kaskita
    ports: ["5432:5432"]
    volumes: ["pgdata_dev:/var/lib/postgresql/data"]

volumes: { pgdata_dev: {} }
